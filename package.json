{
  "name": "prototypes",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "plugin-helpers": "NODE_OPTIONS=--openssl-legacy-provider node ../../scripts/plugin_helpers",
    "kbn": "NODE_OPTIONS=--openssl-legacy-provider node ../../scripts/kbn",
    "build": "yarn plugin-helpers build",
    "dev": "yarn plugin-helpers dev",
    "dev:watch": "yarn dev --watch",
    "bootstrap": "yarn kbn bootstrap && yarn install"
  }
}
